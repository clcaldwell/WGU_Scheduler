<UserControl
    x:Class="Scheduler.View.CustomerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:Scheduler"
    xmlns:ViewModel="clr-namespace:Scheduler.ViewModel"
    xmlns:xctk="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
    mc:Ignorable="d"
    d:DesignWidth="1000" Height="550">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:CallMethodAction TargetObject="{Binding}" MethodName="LoadCustomers"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="AliceBlue" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Height" Value="20" />
        </Style>
    </UserControl.Resources>

    <Grid Background="WhiteSmoke">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="15"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="15"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="1" Grid.ColumnSpan="7" Grid.Row="2">
            <TextBlock Text="Customers" FontSize="30" FontWeight="Bold" Margin="5"/>
            <DataGrid Name="CustomerssDataGrid"
                          Grid.Column="1" Grid.ColumnSpan="5" Grid.Row="2" 
                          Height="200" Background="AliceBlue" Margin="5"
                          ItemsSource="{Binding AllCustomersLoaded}"
                          CurrentItem="{Binding CurrentAppointment, Mode=OneWayToSource}"/>
        </StackPanel>
        
        <Label Content="Selected Customer" FontWeight="Bold" Grid.Column="2" Grid.ColumnSpan="2" Grid.Row="3" Margin="2"/>
        <StackPanel Grid.Column="2" Grid.Row="4">
            <TextBox Text="Name" IsEnabled="False"/>
            <TextBox Text="Address" IsEnabled="False"/>
            <TextBox Text="City" IsEnabled="False"/>
            <TextBox Text="Country" IsEnabled="False"/>
            <TextBox Text="Zip" IsEnabled="False"/>
            <TextBox Text="Phone" IsEnabled="False"/>
        </StackPanel>
        <StackPanel Grid.Column="3" Grid.Row="4">
            <TextBox Name="nameBox" Text="{Binding SelectedCustomer.Name, Mode=TwoWay}" IsEnabled="True"/>
            <TextBox Name="addressBox" Text="{Binding SelectedCustomer.Address, Mode=TwoWay}" IsEnabled="False"/>
            <TextBox Name="cityBox" Text="{Binding SelectedCustomer.City, Mode=TwoWay}" IsEnabled="False"/>
            <TextBox Name="countryBox" Text="{Binding SelectedCustomer.Country, Mode=TwoWay}" IsEnabled="False"/>
            <TextBox Name="zipBox" Text="{Binding SelectedCustomer.Zip, Mode=TwoWay}" IsEnabled="False"/>
            <TextBox Name="phoneBox" Text="{Binding SelectedCustomer.Phone, Mode=TwoWay}" IsEnabled="False"/>
        </StackPanel>


        <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.ColumnSpan="2" HorizontalAlignment="Center" Grid.Row="5">
            <Button Name="saveButton" Content="Save" Command="{Binding SaveCustomerCommand}" CommandParameter="{Binding}"
                Width="40" Height="25" Margin="5" Visibility="Visible"/>
            <Button Name="cancelButton" Content="Cancel" 
                Width="40" Height="25" Margin="5" Visibility="Visible"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Column="5" Grid.Row="3">
            <Button Name="addButton" Content="Add" 
                Height="30" Width="50" Margin="5"/>
            <Button Name="editButton" Content="Edit" Command="{Binding EditCustomerCommand}" CommandParameter="{Binding}"
                Height="30" Width="50" Margin="5"/>
            <Button Name="deleteButton" Content="Delete" 
                Height="30" Width="50" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>